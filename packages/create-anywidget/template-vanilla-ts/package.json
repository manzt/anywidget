{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild --minify --format=esm --bundle --outdir=src/my_widget src/widget.ts",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"esbuild": "^0.19.2",
		"typescript": "^5.2.2"
	},
	"dependencies": {
		"@anywidget/types": "^0.1.4"
	}
}
