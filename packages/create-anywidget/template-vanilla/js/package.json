{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild --minify --format=esm --bundle --outdir=../src/my_widget widget.js"
	},
	"dependencies": {},
	"devDependencies": {
		"esbuild": "^0.18.14"
	}
}
